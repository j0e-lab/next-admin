query Clients {
  allClients {
    id
    name
    created_at
    updated_at
  }
}

query Client($id: Int!) {
  Client(id: $id) {
    id
    name
    created_at
    updated_at
  }
}

# mutation CreateClient($name: String!) {
#   createClient(client: $name) {
#     id
#     name
#     created_at
#     updated_at
#   }
# }
